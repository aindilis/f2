#!/usr/bin/perl -w

# the way to package the most software

# take a list of all the ai projects by scraping sourceforge

# get download urls

# download the latest releases

# put them in a directory and run the software for making packages

# doesn't work because we'll be missing lots of software

# pass standard IQ tests
